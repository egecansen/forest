/**
 * Placeholder — Task 11 replaces this with the real operator-notification
 * implementation (e.g. desktop/webhook notify when a decision is needed).
 */
export const notifyDecision = (_: string) => {};
